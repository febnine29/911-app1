import React, { FC, useState } from 'react';
import {Text} from 'native-base'
const MainPage: FC = () =>{
    return (
        <Text fontSize={20} textAlign='center'>Home Page</Text>
    )
}
export default MainPage